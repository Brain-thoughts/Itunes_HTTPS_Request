# Itunes_HTTPS_Request
This program creates a HTTPS request to pull data on Itunes artist(s). Using requests library to pull the HTTPS, json library for data formatting, and sys to interact with the user. 
